#include "Bricks.h"



/*
#include "AppTile.h"

	AppTile::AppTile(string textureName, float x, float y, bool passable)
	{
		pos = Vector2f(x, y);
		sprite.setPosition(pos);
		isPassable = passable;
	}

	bool AppTile::setUpSprite(string name)
	{
		if (!texture.loadFromFile(name))
		{
			return false;
		}
		texture.setSmooth(true);
		sprite.setTexture(texture);
		sprite.setTextureRect(IntRect(0, 0, 50, 50));
	}
	


	*/